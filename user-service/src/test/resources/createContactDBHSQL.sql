CREATE TABLE users (id INTEGER GENERATED BY DEFAULT AS IDENTITY(START WITH 1, INCREMENT BY 1), userId VARCHAR(100) NOT NULL PRIMARY KEY, firstName VARCHAR(100) NOT NULL, lastName VARCHAR(100), password VARCHAR(100) NOT NULL);

